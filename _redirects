/* This file is intentionally left blank. */

/* Fallback SPA: servir index.html para todas las rutas (cliente-side routing)
    /index.html   200

# Ejemplo opcional: forzar HTTPS y dominio (descomentar y adaptar)
#     /*  https://example.com/:splat   301!

# Ejemplo de redirección permanente
# /ruta-antigua   /ruta-nueva   301